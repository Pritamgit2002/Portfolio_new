import React from "react";
import Home from "../components/home/page";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Home />
    </div>
  );
};

export default page;
